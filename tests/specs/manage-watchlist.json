[
  {
    "name": "Manage Watchlist",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Private watchlist storage isn't set up yet. Try again after it is configured."
            }
          }
        ],
        "send": {
          "callback": "menu:manage-watchlist"
        }
      }
    ]
  }
]
